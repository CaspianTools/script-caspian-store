node_modules/
dist/
firebase/functions-admin/lib/
firebase/functions-email/lib/
firebase/functions-stripe/lib/
firebase/functions-instagram/lib/
firebase/functions-pos/lib/
# Claude Code local state. Ignore everything under .claude/ EXCEPT a committed
# settings.json, so shared worktree/session config can be tracked while the
# isolated worktrees (.claude/worktrees/, parallel sessions) never surface as
# untracked files in the main checkout.
.claude/*
!.claude/settings.json
.claude/worktrees/
.vscode/
package-lock.json
examples/**/next-env.d.ts
*.tgz
.DS_Store
*.log
.env
.env.local
.env.*.local
service-account.json
serviceAccountKey*.json
# POS licence signing key. Losing it means no new licences can ever be minted;
# committing it means anyone can mint their own.
pos-signing-key*.pem
*-signing-key.pem
credentials.json
.turbo/
.next/
.vite/
.cache/
coverage/
*.tsbuildinfo
